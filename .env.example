# Copy to .env and fill in. .env is gitignored.
OPENAI_API_KEY=sk-...
# Optional: point at an OpenAI-compatible endpoint (e.g. Azure/proxy). Leave unset for api.openai.com.
OPENAI_BASE_URL=
# Optional: only needed for gated HF models. Qwen2.5-7B-Instruct and no_robots are public.
HF_TOKEN=
